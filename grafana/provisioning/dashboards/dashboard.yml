apiVersion: 1

providers:
  - name: "Hardware Fleet"
    orgId: 1
    folder: "Redfish Infrastructure"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
